<div class="container">
  <div class='row'>
    <div class='col-xs-12 col-md-6 col-md-offset-3 footer-container'>

      <div class="form">
      <% if params[:as] == "client" %>
          <%= render "form_client" %>
        <% else %>
          <%= render "form_pilot" %>

      <% end %>
      </div>
    </div>
  </div>
</div>
