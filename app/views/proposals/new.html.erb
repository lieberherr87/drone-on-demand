<div class="container">
  <div class='col-xs-12 col-md-6 col-md-offset-3 footer-container'>
    <div class="form">
      <h1>Make a Proposal for <%= User.find_by(id:@request.user_id).first_name %> <%= User.find_by(id:@request.user_id).last_name %></h1>
      <%= render "form" %>
    </div>
  </div>
</div>
