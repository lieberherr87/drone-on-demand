<div class="container">
  <h1><%= @profile.company_name %>'s Profile</h1>
  <p>Check out our Profile. We're ready to amaze you!</p>

  <p><%= @profile.description %></p>

  <% @profile.images.each do |p| %>
    <%= cl_image_tag p.image %>
  <% end %>

</div>
